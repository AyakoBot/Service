export enum ComponentBuilderCommand {
 ComponentBuilder = 'component-builder',
}

export enum ComponentBuilderSubcommand {
 Create = 'create',
 ViewMessage = 'view-message',
 ViewSaved = 'view-saved',
}
